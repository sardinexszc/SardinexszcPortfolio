NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
# Required in production. Use the canonical HTTPS URL without a trailing slash.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
# Optional: hostname for externally hosted project screenshots, for example cdn.example.com.
PROJECT_IMAGE_HOST=
